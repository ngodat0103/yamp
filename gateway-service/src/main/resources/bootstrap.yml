spring:
  application:
    name: gateway
  cloud:
    gateway:
      mvc:
        routes:
          - id: auth-service
            uri: http://auth-service:8001
            predicates:
              - Path=/account/**
server:
  port: 8000
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.security.: DEBUG
